/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  devIndicators: false,
  // Enable static exports if desired, otherwise remove this
  // output: 'export',
}

export default nextConfig

